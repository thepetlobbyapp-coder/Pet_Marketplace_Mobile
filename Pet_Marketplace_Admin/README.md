# Pet Marketplace — Admin

Painel administrativo web. **Next.js + TypeScript**.
Visualizar/bloquear usuários, moderar denúncias e avaliações, auditoria.

> Bloco 0 entrega apenas o esqueleto. A inicialização do Next.js é o **Bloco 9**.

- Documentação oficial: `./docs/` (cópia sincronizada — editar na raiz).
- Agentes: `./.codex/`.
- RBAC obrigatório em todas as rotas administrativas.
- Textos via i18n `en-GB`. **Sem texto hardcoded.**
- Não configurar deploy/Vercel agora (decisão posterior).

## Scripts (placeholder até Bloco 9)

```bash
pnpm install
pnpm dev      # definido no Bloco 9 (Next.js)
```
