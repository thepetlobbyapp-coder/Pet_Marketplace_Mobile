# Mobile — copie para .env (NÃO commitar .env). Só variáveis públicas.
APP_DEFAULT_LOCALE=en-GB
# Dev pode ser http://localhost:3000. PRODUÇÃO: obrigatório HTTPS (gate MOBILE_SECURITY).
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
